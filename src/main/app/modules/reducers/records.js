import {GET_RECORDS_REQUEST, GET_RECORDS_SUCCESS, GET_RECORDS_FAILURE} from '../actions/records';const initialState = () => {    return {        isFetching: false    }};const records = (state = initialState(), action) => {    switch (action.type) {        case (GET_RECORDS_REQUEST):            return {                ...state,                isFetching: true            };        case (GET_RECORDS_SUCCESS):            return {                ...state,                isFetching: false,                data: action.data            };        case (GET_RECORDS_FAILURE):            return {                ...state,                isFetching: false,                message: action.message            };        default:            return state;    }};export default records;