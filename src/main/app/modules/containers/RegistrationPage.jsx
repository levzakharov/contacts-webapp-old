import React from 'react';import {connect} from 'react-redux';import UserForm from '../components/UserForm';import Notification from '../components/common/Notification';import {register} from '../actions/registration';class RegistrationPage extends React.Component {    constructor(props) {        super(props);    }    render() {        const {registration: {errorMessage}} = this.props;        return (            <div>                <h1>Registration</h1>                {errorMessage &&                    <Notification style="danger" header="Error!" text={errorMessage}/>                }                <UserForm onSubmit={this.props.register} buttonText="Sign up"/>            </div>        )    }}function mapStateToProps(state) {    const {registration} = state;    return {        registration    };}export default connect(mapStateToProps, {register})(RegistrationPage);