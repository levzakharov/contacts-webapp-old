import React from 'react';import {Table} from 'react-bootstrap';import {connect} from 'react-redux';import {getRecords} from '../actions/records';const propTypes = {    authentication: React.PropTypes.shape({        isAuthenticated: React.PropTypes.bool.isRequired,        token: React.PropTypes.string    })};class Contacts extends React.Component {    constructor(props) {        super(props);    }    componentDidMount() {        this.props.getRecords(this.props.authentication.token);    }    render() {        const {data} = this.props.records;        return (            <div>                <h1>Contacts</h1>                <Table striped bordered condensed hover>                    <thead>                    <tr>                        <th>Name</th>                        <th>Phone</th>                    </tr>                    </thead>                    {data && (                        <tbody>                        {data.map(person => (                            <tr key={person.id}>                                <td>{person.name}</td>                                <td>{person.phone}</td>                            </tr>                        ))}                        </tbody>                    )}                </Table>            </div>        )    }}function mapStateToProps(state) {    const {authentication, records} = state;    return {        authentication,        records    };}Contacts.propTypes = propTypes;export default connect(mapStateToProps, {getRecords})(Contacts);